Gaétan - vérifier que toute classe est définie
    -> OK
Gaétan - vérifier que le graphe d'héritage est sans circuit
    -> OK
Thomas - vérifier que les classes string et integer sont sans sous-classes
    -> OK
Arthur - vérifier que this/super utilisé dans méthodes de classes
    -> OK
Angel - nom constructeur = nom classe
    -> OK

analyse de portée : quelle variable déclarée / visible o`u
vérififer que les identificateurs ont été déclarés
identifier qui est qui : quel id est masqué (decl la plus récente masque la précédente)

env : {[x: integer] -> [y:int] -> [x:string] -> [id : type]} ...
env : attributs classe, identificateurs param, quand dans un bloc : ajoute en tête, quand quitte le bloc : supprime autant qu'ajouté pour le bloc
modifications de portée / d'env : que dans un bloc

doit savoir le type de this

super : savoir dans quelle classe on se situe
result : si dans corps du'une méthode
si type de retour -> du type de retour, sinon result interdit


Après, typage:
x + y ok quand ?
x.f(3, "cc") ok ?
vérif si x existe
si une méthode f existe
vérif le type des arguments
si x de classe c, il doit y avoir une méthode f dans la classe c /superclasse

le f -> méthode directement ou de façon héritée


vc sur ast classes
    déf attributs corrects
    au niveau de chaques fonctions

vc sur bloc
